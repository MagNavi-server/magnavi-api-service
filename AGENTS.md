# AGENTS.md — MagNavi API Service

이 저장소에서 작업하는 코딩 에이전트의 지침이다. 상세 설계는 연결된 문서를 기준으로 확인한다.

## 1. 먼저 지킬 작업 원칙

- 코드 수정 전에는 변경 범위·이유·검증 방법을 설명하고 반드시 사용자에게 승인을 받는다. 확인·설명·진단 요청을 구현 승인으로 해석하지 않는다.
- 승인된 범위만 수정한다. 문서 작성 요청을 기능 구현이나 실행 설정 변경의 승인으로 확대하지 않는다.
- 작업 전에 현재 폴더·Git 상태·기존 변경을 확인한다. 사용자 변경과 무관한 파일을 덮어쓰거나 되돌리지 않는다.
- 사용자 요청이나 승인 없이 커밋·푸시·이슈/PR 생성·머지·배포·GitHub 설정 변경을 하지 않는다.
- 의존성 추가, API/gRPC 계약 변경, 모델·DB 변경이 승인 범위를 벗어나면 영향과 선택지를 설명하고 다시 확인한다.
- 설명과 문서는 한국어로 작성하고, 초보자도 이해할 수 있도록 용어의 의미와 변경 이유를 설명한다.

## 2. 저장소 역할과 참고 문서

- 중앙 저장소: `MagNavi-server/magnavi-api-service`, 개인 포크: `ychoik/magnavi-api-service`.
- 목표 역할은 회원·인증·장소 검색·즐겨찾기·업무 DB 관리와 앱 WebSocket ↔ 모델 gRPC 스트리밍 중계다. 모델 계산은 Python이 담당한다.
- 현재는 시작 프로젝트와 모듈별 빈 클래스 중심이다. 클래스가 존재한다고 API·인증·DB·gRPC가 구현되었다고 판단하지 않는다.
- 작업 전에 [프로젝트 개요](PROJECT_OVERVIEW.md)와 [구현 계획](IMPLEMENTATION_PLAN.md)의 관련 부분을 읽는다.
- 모듈 변경은 [모듈 안내서](docs/modules/README.md)와 해당 모듈 문서를, DB 변경은 [DB 스키마 설계](DATABASE_SCHEMA.md)를 확인한다.
- Git 작업은 [커밋 컨벤션](docs/convention/commit-convention.md)과 [브랜치·PR·배포 규칙](docs/convention/branch-convention.md)을 확인한다.

## 3. Spring·모듈·DB 구현 기준

- 가벼운 DDD와 모듈러 모놀리스를 유지한다. 하나의 Spring 애플리케이션 안에서 member·place·favorite·positioning을 업무 모듈로 구분한다.
- 요청 처리·업무 흐름·도메인 규칙·외부 기술 구현의 책임을 나눈다. 다른 모듈의 JPA 저장소나 내부 엔티티에 직접 의존하지 않고 공개 기능·필요한 ID로 협력한다.
- shared는 최소 공통 지원, config는 설정 조립 영역이다. 모듈별로 별도 서버·DB·컨테이너를 만들지 않는다.
- DB 트랜잭션은 필요한 업무 처리 범위로 제한한다. WebSocket·gRPC 스트림 수명 전체를 트랜잭션으로 감싸지 않는다.
- 스키마 변경은 Flyway 기준으로 관리하고 JPA는 검증하는 방향을 따른다. 운영 `ddl-auto=update`나 적용된 마이그레이션의 임의 수정으로 우회하지 않는다.
- 카카오·구글 계정은 검증된 제공자 식별자로 연결한다. 이메일이 같다는 이유만으로 서로 다른 계정을 자동 병합하지 않는다.
- 네이버 검색 결과는 자동으로 DB에 누적하지 않는다. 외부 즐겨찾기 저장은 허용 범위·식별 방식이 확인된 정책만 구현한다.
- Spring은 모델 호출의 gRPC 클라이언트다. 재사용 채널과 세션별 스트림을 구분하고, 모델 계산이나 16개 센서 윈도우를 중복 구현하지 않는다.
- 측정 연결마다 인증·순서·입력/결과 큐 한도·종료를 관리한다. 취소를 gRPC까지 전파하고 이전 세션 결과를 재접속한 앱에 전달하지 않는다.
- 모델 장애가 독립적인 회원·장소 API까지 막지 않게 한다. 생성된 gRPC 코드는 직접 고치지 않고 계약·버전·양쪽 배포 순서를 확인한다.
- 승인 없이 Python 저장소나 모바일 코드를 함께 수정하지 않는다. 연동 변경이 필요하면 상대 서버의 변경 범위를 먼저 설명한다.

## 4. 코드·주석·문서 작성

- 가독성을 우선하고 이름으로 역할을 드러낸다. 한 번에 무관한 리팩터링이나 전체 포맷 변경을 섞지 않는다.
- 각 클래스와 메서드·함수에 역할을 설명하는 주석을 작성한다. 중요한 코드에는 처리 이유·제약·예외 상황을 설명한다.
- 이름을 반복하는 주석보다 책임·입력 조건·실패 처리·자원 정리 이유를 적는다. 동작 변경 시 기존 주석도 맞춘다.
- 설계 문서를 기능 구현의 증거로 취급하지 않는다. 계획·빈 골격·구현·검증 완료를 구분한다.
- 동작이나 설계가 바뀌면 관련 문서를 승인 범위 안에서 갱신한다. 미정 항목을 임의로 확정하거나 예시를 실제 설정으로 표시하지 않는다.

## 5. 검증과 실행 주의사항

- 실제 `build.gradle`·Gradle Wrapper·테스트 설정을 먼저 확인한다. 현재 선언된 Java toolchain은 21이다.
- 코드 변경 시 격리된 테스트 환경을 준비하고 프로젝트 루트에서 `./gradlew test`, 필요하면 `./gradlew build`로 관련 검증을 수행한다.
- 현재 DB·외부 서비스 설정이 완성됐다고 가정하지 않는다. Gradle 해석이나 테스트 기동이 실패하면 원인을 보고하고, 임의 버전 변경·테스트 비활성화로 통과시키지 않는다.
- 빈 클래스의 `javac` 성공은 전체 Gradle 빌드·Spring 기동·업무 기능 검증과 다르다. 실행한 범위를 정확히 보고한다.
- 인증은 위조·만료·타인 접근, DB는 트랜잭션·고유/FK 제약, 실시간은 세션 격리·취소·과부하 등 변경과 관련된 실패 경로도 검증한다.
- DB 테스트는 운영 DB 대신 격리 MySQL을 사용한다. 모델 없이 검증할 테스트와 실제 모델 연동 검증을 구분한다.
- 문서만 바꾸면 링크·제목·코드 블록·공백과 `git diff --check`를 확인한다. 미추적 파일은 본문도 직접 검토한다.
- 테스트 도구가 없거나 외부 연결이 필요한 경우 검증하지 못한 항목과 이유를 남긴다. 실제 운영 비밀값을 받아오는 것을 기본 해결책으로 삼지 않는다.

## 6. Git·이슈·PR

- 상세 규칙은 위에 연결한 커밋·브랜치 문서를 따른다. 문서에 작업 절차가 있다는 이유만으로 GitHub 작업이 승인된 것은 아니다.
- Git 작업이 요청된 경우 실제 remote와 상태를 확인한다. 기본 흐름은 중앙 이슈 → 최신 `upstream/develop` 기준 새 작업 브랜치 → 개인 포크 `origin`에 push → 중앙 `develop`으로 PR이다.
- 브랜치는 `타입/실제이슈번호-영문-작업설명`, 커밋은 `type(scope): 한국어 요약`을 사용한다. 번호를 임의로 만들지 않는다.
- 공용 `main`·`develop`·`release`에 직접 push하거나 강제 push하지 않는다. 이미 머지한 작업 브랜치에 새 작업을 계속 쌓지 않는다.
- 작업 PR은 Squash, 공용 브랜치 간 PR은 Merge commit을 사용한다. 필수 승인 인원은 0명이며, 개인 작업 브랜치는 머지 후에도 보존한다.
- 이슈·PR 본문을 채팅에 제공할 때는 본문 전체를 하나의 Markdown 코드 블록에 담아 한 번에 복사할 수 있게 한다. GitHub에 게시할 실제 본문은 일반 Markdown으로 작성한다.
- 완료한 이슈만 `Closes #번호`, 일부 작업은 `Refs #번호`로 구분한다. 서버별 이슈 번호를 혼동하지 않고 실제 종료 여부를 확인한다.
- 관련 파일만 선택해 스테이징한다. IDE 설정·임시 파일·비밀값을 함께 올리거나 기존 이력을 임의로 재작성하지 않는다.

## 7. 보안·데이터·Docker

- 비밀번호·토큰·API 키·운영 접속 정보는 코드·문서·이미지·로그에 넣지 않는다. 기존 비밀값을 발견해도 출력에 재노출하지 않는다.
- 센서 원본과 상세 예측 결과는 DB에 저장하지 않고 운영 기본 로그에도 남기지 않는다.
- 승인 없이 운영 DB에 연결해 데이터를 변경하거나, 초기화·마이그레이션·백업 복원을 실행하지 않는다.
- DB 볼륨 삭제·데이터 손실·기존 변경 폐기 가능성이 있는 작업은 정확한 대상과 영향을 설명하고 별도 승인을 받는다.
- Docker 목표 구성은 Spring·Python·MySQL 각각의 컨테이너와 Compose다. 서버 배포 때문에 DB·볼륨을 초기화하지 않는다.
- EC2 배치, 이미지 저장소, Compose 관리 위치는 아직 미정이다. 실제 구현 상태는 파일로 확인하고 Docker·CI/CD가 이미 구축됐다고 가정하지 않는다.
- 운영 DB·모델 서버는 필요한 내부 통신으로 제한하고 비밀값은 외부에서 주입한다. 배포는 검증된 이미지로 대상 서버만 교체하는 방향이다.
- `release` 중심 CI/CD 계획은 수동 배포 권한을 뜻하지 않는다. 배포·데이터 삭제는 문서 작업과 분리한다.

## 8. 작업 결과 보고

변경한 파일과 핵심 내용, 실제 실행한 검증과 결과, 실행하지 못한 검증과 이유를 구분해 보고한다. 커밋·푸시·배포를 하지 않았다면 완료한 것처럼 표현하지 않는다. Notion 기록 대상 작업이면 게시 링크와 완료·보류 여부도 보고한다.

## 9. Notion 개발·트러블슈팅 기록

- 기록 대상은 사용자가 지정한 [마그네비 고도화 과정](https://app.notion.com/p/3dfdedc0d7818098a812e43d1a162342) 페이지다. 페이지 ID는 `3dfdedc0-d781-8098-a812-e43d1a162342`이다.
- 이 프로젝트에서 의미 있는 구현·구조 변경·설계 결정·트러블슈팅을 수행한 뒤 해당 페이지에 개발 과정을 기록한다. 이 기록은 사용자가 요청한 작업 절차이며, 별도 요청 없이 다른 페이지로 대상을 바꾸거나 공유 범위를 변경하지 않는다.
- 단순 질문 답변, 변화 없는 점검, 경미한 문구·형식 조정은 별도 개발 기록으로 만들지 않는다. 같은 작업을 이어서 진행하면 기존 항목을 갱신하고 중복 기록을 피한다.
- 쓰기 전에 페이지의 최신 내용을 읽는다. 기존 본문·사용자 메모·하위 페이지를 보존하고 해당 항목만 추가하거나 수정한다.
- 항목은 날짜(한국 시간)·대상 서버·작업 제목으로 구분한다. 실제 작업일을 알 수 없으면 정리일과 과거 작업 기록임을 표시하고 날짜를 추정하지 않는다.
- 기록은 작업당 5~8줄 내외의 짧은 요약을 기본으로 한다. 장문의 회고나 전체 작업 일지를 반복해서 쌓지 않는다.
- 목적·문제 → 핵심 변경·해결 방법 → 검증 결과 → 남은 작업 → 참고 링크 순서로 각 항목을 한두 문장으로 정리하고, 해당하지 않는 항목은 생략한다.
- 상세 설계·코드·전체 오류 로그·시도 과정을 복사하지 않고 필요한 기존 MD·이슈·PR을 참조한다. 자세한 설명은 사용자가 요청하거나 중요한 위험·미해결 사항을 설명하는 데 꼭 필요한 경우에만 추가한다.
- 설계·문서 작성·빈 구조 준비·기능 구현·검증·배포 상태를 구분한다. 미구현 계획을 구현 완료로 기록하지 않고, 가설과 확인된 원인도 구분한다.
- 트러블슈팅은 증상·확인된 원인·핵심 해결 방법·검증 결과를 중심으로 요약한다. 재현 조건·대안·재발 방지는 이해에 꼭 필요한 내용만 남기고, 해결되지 않았으면 미해결로 표시한다.
- 실제 실행한 명령·검증 결과와 실행하지 못한 항목·이유를 분리한다. 확인하지 않은 성능 수치·테스트 성공·머지·배포 결과를 만들지 않는다.
- 관련 파일·설계 문서와 실제 존재하는 이슈·PR·커밋 링크를 남긴다. 아직 커밋·푸시하지 않은 파일은 로컬 작업 상태임을 밝히고 존재하지 않는 GitHub 파일 링크를 만들지 않는다.
- 비밀번호·토큰·API 키·개인 연락처·운영 접속 정보·원본 센서·상세 위치·민감한 로그는 기록하지 않는다.
- Notion 연결 도구를 우선 사용한다. 접근할 수 없다면 사용자에게 원인을 알리고, 로그인된 브라우저에서 같은 페이지에 정상 접근할 수 있는지 확인한다. 권한·계정·워크스페이스 공유 설정을 임의로 바꾸지 않는다.
- 연결 도구와 브라우저 모두 사용할 수 없으면 기록 초안을 채팅으로 제공하고 접근 확인을 요청한다. 다른 페이지에 대신 게시하거나 기록 완료라고 보고하지 않는다.
- 게시 후 본문과 대상 페이지를 다시 확인한다. 최종 답변에는 Notion 링크와 기록 완료·보류 여부를 함께 알린다.

이 파일은 작업 지침이며 권한·보호 규칙을 강제하는 설정 파일은 아니다. 파일 형식 참고: [OpenAI AGENTS.md 안내](https://learn.chatgpt.com/docs/agent-configuration/agents-md).
