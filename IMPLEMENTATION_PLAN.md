# MagNavi API Service — Spring 구현 계획

작성일·수정일: 2026-09-10

이 문서는 Spring 저장소에서 수정할 기본 설정과 새로 구현할 업무·DB·실시간 연결 기능을 정리한다. 서비스 목적과 현재 상태는 [프로젝트 개요](PROJECT_OVERVIEW.md)를 참고한다.

모듈별 기능과 요청 흐름이 궁금하면 [모듈 안내서](docs/modules/README.md)부터 읽는다. 상세 설명은 [member](docs/modules/MEMBER.md), [place](docs/modules/PLACE.md), [favorite](docs/modules/FAVORITE.md), [positioning](docs/modules/POSITIONING.md), [shared](docs/modules/SHARED.md) 문서로 나누었다.

테이블·컬럼·FK·고유 제약·저장 정책은 [DB 스키마 설계](DATABASE_SCHEMA.md)를 기준으로 한다. 일반 로그인 유지안에 카카오·구글 로그인과 네이버 지역 검색을 추가하고, 자체 실외 장소 CRUD는 조건부로 조정했다.

Python의 모델 로딩·버퍼·전처리 내부 구현은 이 문서의 범위가 아니다. 모델 서비스와 합의할 입력·출력·오류 계약 및 연동 조건만 다룬다.

현재 Spring 프로젝트는 기존 시작 클래스·기본 테스트에 모듈별 빈 클래스 39개를 추가한 구조 준비 상태다. 새 클래스에는 package·역할 주석·빈 선언만 있다. 아래 기능은 구현 계획이며 API·보안·DB·배포가 완성되었다는 뜻은 아니다.

읽는 순서:

- 1~3장: 초기 설정, 모듈 구조, DB 설계.
- 4~7장: 인증·회원·장소·즐겨찾기 기능.
- 8~9장: 실시간 연결과 공통 오류·운영 처리.
- 10~12장: 작업 순서, 검증, GitHub·배포.
- 13장: 구현 전에 확정할 선택 사항.

## 1. 현재 파일에서 수정·추가할 내용

| 대상 | 현재 상태 | 구현할 내용 |
|---|---|---|
| `build.gradle` | MVC·JPA·Security·gRPC Client 등 선언 | WebSocket, protobuf 코드 생성, 필요한 테스트 의존성·버전 호환 확인 |
| `settings.gradle` | 단일 프로젝트 | 초기에는 단일 빌드 유지, 필요 시 프로젝트 표시 이름 정리 |
| 시작 클래스와 기본 패키지 | `com.example.magnavi_springserver` | 기능 모듈 추가, 패키지 이름을 바꾸려면 테스트·스캔 경로 함께 정리 |
| `application.yaml` | 앱 이름만 설정 | DB·JWT·소셜 인증·네이버·gRPC 주소·한도·Actuator 설정 분리 |
| `src/main/resources/db/migration/` | 빈 폴더, SQL 없음 | Flyway 스키마·데이터 마이그레이션 |
| `src/main/proto/` 또는 계약 의존성 | 계약·생성 설정 없음 | 계약 관리 방식 확정 후 Java 메시지·stub 생성 |
| 회원·장소·즐겨찾기 모듈 | 계층별 빈 클래스만 생성 | 일반/소셜 로그인·실내 장소·외부 검색·즐겨찾기 구현 |
| `positioning` 모듈 | 세션·핸들러·클라이언트의 빈 클래스 | WebSocket과 gRPC 클라이언트 연결 |
| `contextLoads()` 테스트 | 생성된 기본 테스트 | 테스트 전용 DB·설정과 의미 있는 기능 검증 |
| Docker·CI·배포 설정 | 없음 | 빌드·검증·release 배포·롤백 |

### 1.1 환경과 의존성 정리

현재 Java 21, Spring Boot 4.1.1을 선언하고 있다. 실제 Gradle 빌드에서 해당 버전과 gRPC·protobuf 조합이 정상 해석되는지 먼저 확인한다. 문제 확인 없이 일괄 최신 버전으로 올리지 않는다.

- WebSocket starter를 추가하고 현재 MVC 기반에서 연결을 구현한다.
- 소셜 로그인 방식에 필요한 OAuth2 Client·토큰 검증 구성을 검토한다. Resource Server만으로 외부 로그인·가입이 자동 구현되지 않는다.
- 네이버 API HUB·카카오·구글의 환경별 인증 정보를 서버 설정으로 분리하고 외부 장애를 대체할 테스트 어댑터를 준비한다.
- protobuf 플러그인 선언에 더해 protoc·gRPC Java 코드 생성과 소스 경로를 설정한다.
- MySQL 기반 통합 테스트를 위해 Testcontainers 또는 동등한 격리 환경을 준비한다.
- 비밀값은 저장소 밖에서 주입하고 문서에는 변수명과 설명만 남긴다.
- `local`, `test`, 배포 환경의 설정을 분리한다.
- JPA의 운영 스키마 자동 변경 대신 Flyway를 사용하고, 엔티티와 DB 일치를 검증한다.

현재 설정만으로 DB 연결·JWT 검증·로그인 발급·gRPC 호출이 완성되는 것은 아니다. 모델 서비스 없이도 업무 API 테스트를 실행할 수 있게 외부 연동을 대체 가능한 구조로 만든다.

완료 기준: 테스트 환경에서 프로젝트가 기동하고 DB 마이그레이션을 적용할 수 있으며, 운영 비밀값 없이 기본 검증이 가능하다.

## 2. 모듈과 계층 구조

### 2.1 가벼운 DDD와 모듈러 모놀리스

초기에는 하나의 Spring 애플리케이션과 빌드로 시작한다. 패키지를 업무별로 나누고, 서로 어떤 기능을 공개할지 정해 확장하기 쉬운 구조를 만든다.

```text
com.example.magnavi_springserver/
├── member/
│   ├── presentation/     # 회원·로그인 REST API
│   ├── application/      # 일반/소셜 로그인·가입·연결·이름 변경 흐름
│   ├── domain/           # 회원 규칙, 저장소·토큰 기능의 인터페이스
│   └── infrastructure/   # JPA·해싱·JWT·소셜 제공자 어댑터
├── place/                # 실내 장소·모델 매핑·네이버 검색·선택적 자체 장소
├── favorite/             # 같은 계층 구분으로 즐겨찾기 구성
├── positioning/
│   ├── presentation/     # WebSocket 요청·결과 변환
│   ├── application/      # 측정 세션 시작·종료, 모델 호출 인터페이스
│   ├── domain/           # 연결 상태·메시지·상태 전이 규칙
│   └── infrastructure/   # gRPC 클라이언트·세션 저장 구현
├── shared/               # 공통 오류·최소 인증 정보 등 지원 코드
└── config/               # 보안·연결·환경 설정을 조립하는 지점
```

위 패키지 구조와 역할별 빈 클래스는 생성했다. Repository·ModelStreamClient 같은 인터페이스는 아직 없고, 현재 클래스에 필드·메서드·어노테이션·상속·구현 선언을 추가하지 않았다. PersistenceAdapter는 나중에 저장소 계약을 구현할 클래스 자리이며 JPA Repository 인터페이스를 대신 구현한 상태가 아니다.

설정 클래스도 어노테이션이 없으므로 자동 설정을 추가하지 않는다. 테스트·마이그레이션은 빈 폴더만 준비하며, .gitkeep과 더미 테스트·빈 SQL은 생성하지 않는다. src/main/proto는 계약 관리 방식 확정까지 보류한다. 기능에 필요한 경계와 인터페이스는 실제 구현 단계에서 추가한다.

업무 모듈은 member·place·favorite·positioning이다. shared는 공통 지원 영역이고 config는 설정을 연결하는 조립 지점이다. shared가 업무 모듈 내부를 참조하지 않도록 하고, 모듈 간에는 공개 Java 기능으로 호출한다. 별도 서버인 모델 서비스와 통신할 때만 gRPC를 사용한다.

- 컨트롤러·WebSocket 핸들러는 형식 변환과 업무 호출에 집중한다.
- 업무 서비스는 가입·등록·삭제 같은 사용 사례를 실행한다.
- 도메인은 소유권·중복·상태 변화 같은 업무 규칙을 표현한다.
- JPA·JWT·gRPC 구현은 외부 기술을 연결한다.
- 다른 모듈의 JPA 저장소를 직접 사용하는 대신 공개된 조회·업무 인터페이스를 호출한다.
- 다른 모듈의 엔티티를 광범위하게 참조하기보다 필요한 ID와 조회 결과를 사용한다.

이 구조가 반드시 도메인 객체와 JPA 엔티티를 모두 두 벌로 작성해야 한다는 뜻은 아니다. 단순 엔티티에 허용할 JPA 의존 범위를 정하고 일관되게 적용한다.

클래스·메서드에는 역할과 중요한 조건을 설명하는 주석을 작성한다. 특히 권한, 트랜잭션 경계, 연결 종료, 순서 보장 코드는 이유를 남긴다.

### 2.2 DB 트랜잭션을 어디에 둘 것인가?

예를 들어 회원가입 중 회원 정보 등록까지는 성공했지만 뒤의 필수 DB 작업이 실패했다면, 같은 가입 처리의 DB 변경이 일부만 남지 않게 해야 한다. 이런 단위를 업무 서비스의 트랜잭션으로 묶는다.

- 등록·수정·삭제는 서비스 진입점에서 트랜잭션 경계를 명시한다.
- 조회는 필요한 범위에 읽기 전용 트랜잭션을 적용한다.
- 같은 클래스 안의 자기 호출만으로 새 트랜잭션이 적용된다고 가정하지 않는다.
- 어떤 예외에서 롤백할지 명시한다. 기본 동작은 모든 예외의 자동 롤백이 아니다.
- 오류를 잡고 정상 반환해 실패한 작업이 정상 처리처럼 보이지 않게 한다.

Spring의 기본 프록시 방식에서 자기 호출은 트랜잭션 경계를 새로 적용하지 않으며, 기본 롤백 대상은 RuntimeException과 Error다. 참고: [Spring @Transactional](https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/annotations.html).

gRPC 요청이나 WebSocket 연결 전체를 DB 트랜잭션 안에 두지 않는다. DB 트랜잭션은 이미 전송한 네트워크 메시지를 되돌리지 못하며, 긴 연결 동안 DB 커넥션을 점유할 이유도 없다.

## 3. 데이터베이스 구조 변경 방향

상세 컬럼·자료형·제약·기존 데이터 대응은 [DB 스키마 설계](DATABASE_SCHEMA.md)가 기준이다. 이 장은 구현 작업과 완료 조건을 요약한다.

### 3.1 기본 테이블

| 소유 모듈 | 테이블 | 구현 방향 |
|---|---|---|
| member | members | 프로필·권한·상태, 이메일은 초기안에서 전역 UNIQUE 제외 |
| member | local_credentials | 일반 로그인 ID UNIQUE·비밀번호 해시, member_id PK/FK |
| member | social_accounts | 검증된 소셜 사용자 ID와 회원 연결 |
| place | buildings·floors·indoor_locations | 건물·층·실내 장소 |
| place | model_location_mappings | 모델 키·버전·코드와 장소 연결 |
| favorite | favorites | 자체 ID·소유 회원·유형·대상 참조·중복 기준 |

일반 로그인을 유지하는 기본안은 8개 업무 테이블이다. 회원·인증을 분리하고 소셜 전용 회원의 일반 인증 행은 만들지 않는다.

- local_credentials의 login_id에 UNIQUE를 둔다.
- social_accounts는 provider+provider_user_id, 초기안의 member_id+provider에 UNIQUE를 둔다.
- 층·모델 매핑·즐겨찾기의 조합 고유 제약은 스키마 문서대로 검증한다.
- 내부 장소 대상은 실제 FK로 보호한다. target_key는 FK를 대신하지 않는다.
- 소셜 앱/issuer의 식별 범위가 여러 개인 배포라면 범위 컬럼·고유 키 확장을 먼저 결정한다.

### 3.2 조건부 테이블·컬럼

자체 실외 장소 관리를 선택할 때 outdoor_places와 필요한 place_facilities를 추가한다. 이 선택 전에는 favorites의 outdoor_place_id FK도 만들지 않는다.

외부 장소 즐겨찾기의 표시 정보·중복 판별 키는 저장 허용 정책이 확정된 뒤 구현한다. 네이버에 고유 장소 ID가 있다고 가정하지 않는다. 내부/외부 장소·버스·정류장을 구분하고 기존 앱 유형·ID의 변환을 명시한다.

우리 서비스 refresh_tokens는 재발급·폐기 정책을 정한 뒤 별도 설계한다. 센서·예측 이력·전체 검색 결과 저장 테이블은 만들지 않는다.

### 3.3 Flyway·JPA

- DB 변경은 Flyway 버전 SQL로 관리하고 JPA는 조회·저장을 담당한다.
- ddl-auto=validate로 구조를 검증하는 방향이며 운영 update와 혼용하지 않는다.
- 회원 테이블 → 실내 장소·매핑 → 즐겨찾기 → 선택 기능 순으로 마이그레이션을 나눈다.
- 이미 공유 환경에 적용한 SQL은 새 버전으로 변경한다.
- 실제 MySQL 버전·문자 비교·NULL·FK·CHECK·인덱스를 테스트한다.

### 3.4 이관

users를 members와 local_credentials로 나누고 기존 해시·ID 대응을 확인한다. 이메일만 보고 social_accounts를 생성하지 않는다.

predicted_locations는 실내 기준 정보와 모델 매핑으로 이관한다. 모델의 출력 코드는 DB PK 변경과 별개다.

favorites는 소유 회원·자체 PK·대상 식별자를 분리한다. 원본에 대상 출처가 없으면 임의 추정으로 내부 FK를 만들지 않는다. outdoor_place는 자체 관리 필요성과 기존 앱 데이터를 검토한 뒤 보존·이관 경로를 정한다.

백업 → 중복·건수 조사 → ID 매핑 → 격리 DB 시험 → API 호환 검증 → Spring으로 쓰기 주체 전환 순으로 진행한다. 빈 DB 생성과 기존 DB 이관은 다른 경로다.

완료 기준: 기본 테이블을 재현하고 부분 저장·잘못된 참조·중복을 차단한다. 조건부 기능은 미확정 상태를 숨기지 않고, 선택한 이관 경로의 데이터·로그인·모델 매핑을 검증한다.

## 4. 인증과 접근 권한

### 4.1 일반·소셜 로그인과 서비스 토큰

일반 로그인은 local_credentials의 해시를 검증하고 members를 조회한다. 소셜 로그인은 제공자의 인증 결과를 검증하고 social_accounts로 회원을 찾거나 생성한다. 이후에는 모두 MagNavi 자체 액세스 토큰을 사용한다.

- 비밀번호는 해시만 저장하고 기존 bcrypt 호환을 테스트한다.
- 구글·카카오의 식별자는 제공자가 검증한 사용자 ID이며 이메일이 아니다.
- 이메일이 같아도 자동 병합하지 않는다.
- 앱이 전달한 소셜 ID·이메일만 믿지 않는다. SDK·서버 흐름에 맞는 검증을 구현한다.
- 서명·issuer·허용 client/앱·만료와 선택한 흐름의 state·nonce·PKCE·재사용 방어를 확인한다.
- 제공자 토큰·인가 코드·서명 키는 기본 DB·로그에 보관하지 않는다.
- JWT 키 누락 시 알려진 기본 키로 대체하지 않는다.
- 내부 member_id를 사용할 서비스 토큰과 기존 sub=userId 토큰의 전환을 합의한다.

Resource Server와 JwtDecoder는 서비스 요청의 JWT 검증 기반이다. 회원가입·토큰 발급·외부 소셜 로그인 자체는 별도 구현이다. [Spring Security JWT](https://docs.spring.io/spring-security/reference/servlet/oauth2/resource-server/jwt.html)

소셜 제공자 검증은 [Google 백엔드 인증](https://developers.google.com/identity/sign-in/web/backend-auth), [Kakao 로그인](https://developers.kakao.com/docs/ko/kakaologin/common)을 기준으로 구체화한다.

### 4.2 계정 연결과 수명

연결 기능을 채택하면 기존 회원 재인증과 새 소셜 계정 인증을 모두 요구한다. 타 회원에게 연결된 계정을 가져오지 않는다. 연결 해제 시 마지막 로그인 수단을 제거할지 등 복구 정책도 필요하다.

재발급·로그아웃·탈퇴·회원 상태 변경 후 이미 발급한 토큰의 처리 범위는 별도 결정이다. status 변경만으로 모든 토큰·WebSocket이 즉시 폐기된다고 가정하지 않는다.

### 4.3 API별 권한

| 요청 | 초기 방향 |
|---|---|
| 일반 가입·로그인·소셜 로그인 | 서비스 로그인 전 접근 가능, 입력·인증 결과·호출 한도 검증 |
| 계정 연결 | 기존 회원 재인증과 새 제공자 인증 |
| 내 정보·이름 변경·즐겨찾기 | 인증한 본인 |
| 실내 장소 조회·외부 검색 | 공개 여부 결정, 검색은 별도 호출 한도 |
| 자체 실외 장소 변경 — 선택 | 관리자 등 쓰기 권한 |
| 실시간 측정 | MagNavi 토큰 인증 후 허용 |
| 운영 기능 | 운영 접근 제한 |

인증과 소유권 검사는 구분한다. 소셜 제공자 ID를 favorite나 positioning의 회원 ID로 사용하지 않는다.

공개·보호 경로와 401·403 응답을 명확히 하고, CORS·CSRF는 실제 헤더·쿠키·리다이렉트 방식에 맞춘다. 제공자 장애를 잘못된 사용자 비밀번호와 같은 오류로 처리하지 않는다.

완료 기준: 일반·소셜 로그인 검증, 잘못된 대상 앱·만료·위조 차단, 타인 정보 접근·연결 탈취 방지를 확인한다.

## 5. 회원 기능

### 5.1 일반 가입·로그인

- 기존 userId·userName·email·phone_number·password를 이관 기준으로 확인한다.
- 연락처 필수·이메일 중복 정책은 소셜 회원을 고려해 다시 합의한다.
- 프로필과 일반 인증 정보를 같은 가입 트랜잭션으로 저장한다.
- 로그인 ID 동시 중복을 DB 제약과 오류 응답으로 처리한다.
- 폼 username·password와 access_token·token_type의 기존 계약을 검토한다.
- 소셜 전용 회원에게 일반 로그인 ID가 없는 경우의 내 정보 응답을 정의한다.

### 5.2 소셜 최초 로그인·재로그인

제공자 검증 → 연결 조회 → 기존 회원 사용 또는 회원·연결 동시 생성 → 회원 상태 확인 → 서비스 토큰 발급 순으로 구현한다.

외부 검증 호출은 짧은 DB 저장 트랜잭션 밖에 둔다. 동시 최초 로그인 충돌 시 신규 회원 부분 저장을 롤백하고 확정된 연결을 조회한다. 필요한 제공자 정보만 수집한다.

### 5.3 내 정보·이름·연결

인증된 내부 회원 ID로 내 정보를 조회하고 표시 이름만 수정한다. 소셜 프로필과 서비스 표시 이름을 매 로그인마다 무조건 덮어쓰지 않는다.

계정 연결·해제 API는 초기 포함 여부를 결정한 뒤 구현한다. 연결 없이 별도 가입한 두 계정을 이메일만으로 합치지 않는다.

완료 기준: 일반·소셜 계정의 저장 분리와 로그인, 동시 가입, 선택한 연결 정책, 내 정보 응답 호환을 테스트한다.

## 6. 장소 기능

### 6.1 실내 장소와 모델 매핑

건물·층·실내 장소의 목록·상세와 페이지 한도를 구현한다. model_key·model_version·location_code로 장소를 연결하고 누락을 임의 기본 장소로 숨기지 않는다. 자체 매핑 캐시는 버전별 갱신을 설계한다.

기존 /locations는 장소 기준 정보이며 예측 이력이 아니다. 실내 관리자 API는 별도 기능이고 초기 데이터는 검증된 마이그레이션·관리 절차로 준비할 수 있다.

### 6.2 네이버 외부 검색

- 앱의 지도 표시와 별개로 place에서 지역 검색을 중계한다.
- PlaceSearchService와 네이버 호출 어댑터를 분리한다. 이름은 예시다.
- 신규 신청·주소·인증 헤더는 NAVER API HUB 기준으로 설정한다.
- 검색어 길이·요청 빈도·응답 대기 시간·재시도 횟수를 제한한다.
- Client Secret과 원문 검색 결과를 앱 설정·일반 로그에 노출하지 않는다.
- 표시 정보·좌표의 앱 계약 변환과 실제 응답을 검증한다.
- 검색만으로 자체 DB에 결과를 저장하지 않는다.
- 검색 장애·호출 한도·정상 빈 결과를 구분한다.

현재 지역 검색은 최대 5개·start=1이며 고유 장소 ID 필드가 없다. 앱 검색 화면의 요구를 충족하는지 먼저 확인한다. [API 명세](https://api.ncloud-docs.com/docs/naver-api-hub-search-local)

저장·캐시·표시 조건은 적용되는 API HUB 약관에서 확인한 후 구현한다. 무제한 영구 저장이나 임의 API 우회·수집을 전제로 하지 않는다.

### 6.3 자체 실외 장소 CRUD — 선택

직접 관리할 장소·시설·접근성 정보가 필요하면 기존 기능을 이관한다. 좌표·권한·시설 참조·PATCH 누락/null·참조 중 삭제 정책을 적용한다.

선택하지 않으면 outdoor_places·place_facilities와 해당 FK는 보류한다. 단, 기존 앱과 데이터를 조사하지 않고 삭제하거나 API 전환을 완료했다고 처리하지 않는다.

완료 기준: 실내 조회·매핑과 외부 검색이 독립적으로 동작한다. 검색 실패와 한도를 검증하고, 자체 장소 관리·기존 데이터 전환 여부를 명확히 정한다.

## 7. 즐겨찾기 기능

- 인증된 내부 member_id를 소유자로 사용한다. 일반·소셜 로그인 방식을 구분해 목록을 나누지 않는다.
- 등록·내 목록·내 항목 삭제와 기존 place·bus·busStop 호환을 구현한다.
- 자체 ID와 대상 키를 구분하고, 내부/외부 장소·버스·정류장 유형의 변환을 정한다.
- 내부 장소는 실제 FK로 연결한다. 선택 outdoor_places가 없다면 그 FK도 추가하지 않는다.
- 네이버의 고유 장소 ID를 가정하지 않는다. 저장 허용 후 자체 중복 키와 한계를 합의한다.
- 외부 제공자의 실제 ID가 있을 때만 external_id에 저장하고 지역·방향 등 식별 범위를 맞춘다.
- 표시 정보는 허용한 필드·기간의 최소 스냅샷만 검토한다. 정책 미확정이면 해당 저장 기능은 보류한다.
- 서버가 만든 대상 키와 UNIQUE 제약으로 회원별 중복을 처리한다.
- 삭제는 id와 member_id를 함께 확인한다.
- 목록은 페이지 한도·안정적인 정렬을 적용하고 항목 수만큼 DB·외부 호출을 반복하지 않는다.

원본 ID와 새 PK의 대응·응답·삭제 API 호환을 유지하거나 전환 시점을 합의한다. 상세 NULL·참조·고유 제약은 DB 스키마 문서가 기준이다.

완료 기준: A·B 동일 대상 저장, A의 중복·타인 항목 삭제 차단, 로그인 수단 연결 후 동일 회원 목록, 조건부 외부 저장·식별 정책을 검증한다.

## 8. WebSocket과 비동기 gRPC 연결

### 8.1 앱과의 통신 방식

현재 MVC 구성을 유지하면서 WebSocket 핸들러를 추가하는 방안을 제안한다. 단순 센서·결과 송수신부터 시작하며, STOMP나 메시지 브로커는 필요가 확인될 때 검토한다.

1. 앱이 측정 연결을 요청한다.
2. 토큰을 검증하고 사용자를 확인한다.
3. 연결 한도를 검사하고 서버가 측정 세션 ID를 만든다.
4. 그 세션에 대응하는 gRPC 스트림을 연다.
5. 센서 입력을 검증해 전달하고 결과를 같은 앱 연결로 보낸다.
6. 종료·만료·오류 시 양쪽 연결과 대기열을 정리한다.

앱 WebSocket 라이브러리가 인증 헤더를 지원하는지 확인한다. 지원하지 않으면 짧은 수명의 연결 티켓 등 대안을 설계하고, 인증되기 전에는 센서를 처리하지 않는다. 장기 JWT를 URL에 넣어 로그에 남기는 방식을 기본으로 삼지 않는다.

### 8.2 측정 세션과 상태

세션은 회원 ID 하나가 아니라 하나의 측정 연결을 나타낸다. 같은 회원이 재접속하거나 여러 기기를 사용하는 경우도 구분한다.

세션에서 관리할 정보:

- 서버가 발급한 세션 ID와 인증된 회원 식별자.
- 앱 WebSocket 연결과 대응 gRPC 호출.
- 토큰 만료 시각, 마지막 활동 시간, 마지막 샘플 순번.
- 연결 상태와 크기가 제한된 입력·결과 대기열.
- 종료·취소 여부.

연결 상태는 인증 확인 → 연결 준비 → 측정 중 → 종료 중 → 종료처럼 명확히 구분한다. 종료 처리가 여러 곳에서 동시에 호출되어도 결과가 달라지지 않도록 멱등적으로 구성한다.

모델용 16개 윈도우는 Python이 관리한다. Spring 세션이 가진 전달 큐를 모델 버퍼와 혼동하지 않는다.

### 8.3 필요한 통신 계약

아래는 필드 초안이다. 실제 필드명·타입·메시지 버전은 양쪽 서버가 함께 확정한다.

| 구분 | 필요한 내용 |
|---|---|
| 연결 시작 | 세션 ID, 계약 버전, 필요한 모델·지도 범위 |
| 센서 | 샘플 순번, 측정 시간, 자기장·방향 각 3개 수치 |
| 예측 | 입력 순번, 모델 버전, 위치 코드, 점수, 후보 목록 |
| 워밍업 | 처리 상태, 모인 샘플 수, 필요한 샘플 수 |
| 오류 | 오류 코드, 앱 안내 메시지, 재시도·재접속 가능 여부 |
| 종료 | 정상 종료·만료·과부하·연결 실패 구분 |

단위·좌표축·측정 시간 기준을 맞추고, 누락과 정상 수치 0을 구분한다. 센서 수치의 NaN·무한대·잘못된 타입과 샘플 중복·역순을 검사한다.

모델 점수를 정확도가 보장된 실제 확률처럼 앱에 표현하지 않는다. 장소 코드는 문자열 등 합의한 형식으로 유지하고 내부 DB ID와의 변환을 명시한다.

계약의 단일 원본과 버전 배포 방식을 정한다. 별도 `magnavi-contracts` 레포를 사용할지는 아직 결정 항목이다. 생성된 Java 메시지·stub을 직접 수정하지 않는다.

### 8.4 gRPC 클라이언트 구현

- 현재 Spring gRPC 버전에 맞춰 비동기 stub을 명시적으로 등록한다.
- 채널은 재사용하고, 측정 세션마다 개별 스트림을 만든다.
- 모델 서버 주소·인증·TLS·메시지 크기를 환경 설정으로 분리한다.
- 연결 준비 실패를 앱 연결 준비 실패로 변환한다.
- 입력 순서를 유지하고 해당 스트림의 결과만 세션에 전달한다.
- gRPC 콜백에서 긴 DB 조회나 무제한 블로킹 송신을 실행하지 않는다.
- 동일한 스트림의 요청 송신은 직렬화하고 클라이언트 흐름 제어를 적용한다.

비동기 stub의 사용은 Spring 전체를 WebFlux로 변경하거나 모든 메서드에 `@Async`를 붙인다는 뜻이 아니다. 사용 중인 생성 코드와 클라이언트 API에 맞춰 구성한다. 참고: [Spring gRPC Client](https://docs.spring.io/spring-grpc/reference/client.html).

Spring은 이번 연동에서 gRPC 클라이언트이므로 추론 호출을 위해 gRPC 서버 역할까지 추가하지 않는다.

### 8.5 동시 송신·과부하와 지연 관리

사용자가 많거나 앱의 결과 수신이 느리면 연결은 살아 있어도 메시지가 쌓일 수 있다.

- 사용자별·인스턴스별 활성 연결 수를 제한한다.
- 로그인·연결 시도·센서 메시지 빈도와 크기를 제한한다.
- 입력·결과 큐 길이와 최대 대기 시간을 제한한다.
- 한 연결의 결과 송신 순서를 보장하고 동시 쓰기를 직렬화한다.
- 모델 또는 앱이 느려지면 흐름 제어를 적용하고 한도를 지속 초과하면 명확히 종료한다.
- 누락을 숨긴 채 센서 샘플을 버리거나 과거 샘플을 자동 재전송하지 않는다.

WebSocket의 기본 세션은 동시 송신을 전제로 하지 않으므로 동기화된 송신 경로나 제한 설정을 갖춘 래퍼 등을 검토한다. 참고: [Spring WebSocket API](https://docs.spring.io/spring-framework/reference/web/websocket/server.html).

서버가 여러 인스턴스라면 인스턴스별 제한만으로 사용자 전체 한도가 보장되지 않는다. 초기 배포 범위를 명시하고, 분산 환경의 전역 제한 저장소는 필요 시 설계한다.

### 8.6 종료·재접속·모델 장애

- 앱 종료와 오류를 gRPC 호출 취소까지 전파한다.
- gRPC 실패 시 앱에 상태를 알리고 해당 측정 세션을 정리한다.
- 토큰 만료·무응답·전송 한도 초과 시 종료 정책을 적용한다.
- 재접속은 새 세션으로 처리하며 이전 연결의 결과를 새 연결에 보내지 않는다.
- 종료 직후 늦게 도착한 결과는 세션 상태와 식별자를 확인해 폐기한다.
- 장시간 측정 스트림에 짧은 단건 RPC 기한을 그대로 적용하지 않는다. 연결 수명·무응답·작업 지연 한도를 구분한다.
- 재접속 재시도에는 횟수·간격 제한을 두어 모델 장애가 반복 연결 폭증으로 이어지지 않게 한다.

모델 서비스 장애가 회원·장소 같은 독립된 REST API의 동작까지 막지 않도록 한다. 업무 API와 실시간 기능의 준비 상태·장애 범위를 구분한다.

완료 기준: 앱 대체 클라이언트 여러 개가 각자 결과를 받고, 한 연결의 종료·지연·오류가 다른 사용자의 상태를 오염시키지 않는다. 종료 후 세션과 대기열이 해제된다.

## 9. 공통 오류·로그·운영 설정

### 9.1 오류 응답

업무 오류는 공통 REST 오류 DTO로, WebSocket 오류는 정의된 이벤트로 변환한다. 통신 방식이 달라도 오류 코드의 의미는 일관되게 유지한다.

| 상황 | 처리 기준 |
|---|---|
| 잘못된 입력 | 필드·오류 코드로 안내 |
| 토큰 없음·만료·위조 | REST 401 또는 연결 인증 실패 |
| 권한 부족 | REST 403 또는 권한 실패 이벤트 |
| 없는 장소·대상 | 404 등 합의한 오류 |
| 로그인 ID·소셜 연결·즐겨찾기 충돌 | 409 등 합의한 충돌 오류 |
| 소셜 제공자·네이버 검색 장애 | 외부 연동 오류, 빈 결과·사용자 인증 실패와 구분 |
| 호출 한도 초과 | REST 429 또는 연결 과부하 이벤트 |
| 모델 준비 실패·연결 실패 | 실시간 기능 사용 불가 안내 |
| 예상하지 못한 오류 | 내부 상세 내용 없이 오류 코드 제공 |

위 HTTP 코드는 목표안이다. 기존 앱의 오류 처리에 영향이 있으면 전환 규격을 합의한다. 예외 메시지·SQL·스택 트레이스를 응답에 그대로 포함하지 않는다.

### 9.2 로그와 지표

- 비밀번호·JWT·연락처·소셜 토큰/인가 코드·API Secret·원문 검색어/결과·센서 원본·상세 예측 결과를 기본 로그에서 제외한다.
- REST 응답 시간, DB 커넥션 상태, 활성 측정 세션 수, gRPC 오류, 큐 길이, 전송 지연을 관찰한다.
- 회원 ID와 세션 ID를 메트릭 라벨에 넣어 시계열 수가 무한히 늘지 않게 한다.
- 진단용 추적 식별자는 업무 개인정보와 구분하고 필요한 보관 기간을 정한다.
- Actuator 엔드포인트는 공개 범위를 제한한다.

### 9.3 기동·종료

필수 DB·JWT 설정은 기동 시 확인한다. 모델 서비스가 일시적으로 사용할 수 없을 때는 실시간 기능의 상태를 따로 표현하고 재연결 정책을 적용한다.

배포 종료 시 신규 연결을 중단하고, 앱 안내·gRPC 취소·대기열·세션 정리를 제한된 시간 안에 수행한다. 네트워크 서버를 재시작한다고 모든 진행 중 연결이 자동으로 복구되는 것은 아니다.

## 10. 구현 순서와 단계별 완료 기준

| 단계 | 주요 작업 | 완료 기준 |
|---|---|---|
| 1. 프로젝트 기반 | 빌드·환경 설정·모듈 경계·공통 오류 | 외부 모델 없이 기본 테스트 가능 |
| 2. DB | 스키마·제약·Flyway·기존 데이터 정책 | 빈 DB 생성과 선택한 이관 경로 재현 |
| 3. 인증·회원 | 일반/소셜 로그인·JWT·내 정보·선택한 연결 기능 | 동시 가입·위조·만료·연결 충돌·타인 접근 검증 |
| 4. 장소 | 실내 조회·모델 매핑·네이버 검색·선택적 자체 장소 | 검색 한도·장애·무저장·선택한 변경 정책 검증 |
| 5. 즐겨찾기 | 내부/외부 유형·소유권·중복·목록·삭제 | 타인 삭제 차단, 허용된 외부 저장·식별 규칙 검증 |
| 6. 계약·모델 호출 | Java 생성 코드·비동기 stub·가짜 모델 연동 | 입력·결과·오류를 계약대로 처리 |
| 7. 앱 실시간 연결 | WebSocket 인증·세션·한도·취소·재접속 | 다중 클라이언트에 각자의 결과 전달 |
| 8. 실제 통합·성능 | 실제 모델 서비스 연동·지연·부하 측정 | 목표 규모·오류율·자원 한도 충족 |
| 9. 배포·전환 | CI/CD·상태 확인·롤백·앱 전환 | 업무 DB 쓰기 주체와 API 경로 전환 확인 |

gRPC 계약 정의는 초기에 병행할 수 있다. 모델 서비스 구현이 끝날 때까지 모든 Spring 작업을 기다릴 필요는 없으며, 가짜 모델 서비스로 정상·지연·오류 응답부터 검증한다.

첫 PR은 생성된 기본 프로젝트와 환경·문서 정리를 담고, 이후 기능 단위로 나눈다. Spring 코드를 올리기 전에는 중앙 develop을 기준으로 작업 브랜치가 연결되어 있는지 확인한다.

각 단계에서 클래스·메서드 주석, 테스트, API 변경 내용을 함께 작성한다. 의존성이 추가되거나 클래스가 생성된 것만으로 기능 완료로 처리하지 않는다.

## 11. 검증 계획

| 대상 | 대표 시나리오 | 기대 결과 |
|---|---|---|
| 빌드·설정 | 테스트 설정과 격리 DB로 기동 | 운영 비밀값·실제 모델 없이 기본 검증 |
| DB | 신규 DB·기존 데이터 이관·고유 제약 | 일관된 스키마·ID·참조 관계 |
| 트랜잭션 | 필수 DB 작업 중 오류 | 선택한 업무 범위가 부분 저장되지 않음 |
| 회원 | 일반 가입의 두 테이블 저장·동시 중복 | 부분 저장 방지, 비밀번호 노출 없이 결과 구분 |
| 소셜 로그인 | 동일 계정 동시 가입·다른 앱 토큰·이메일 일치 | 중복·잘못된 인증 차단, 자동 병합 없음 |
| 인증 | 정상·만료·위조 토큰 | 본인만 보호된 요청 허용 |
| 장소 | 매핑 누락·선택적 자체 장소의 좌표/권한 오류 | 정의된 오류, 임의 장소로 대체하지 않음 |
| 외부 검색 | 빈 결과·한도·시간 초과·API 키 실패 | 결과 없음과 외부 장애 구분, 검색 결과 DB 누적 없음 |
| 외부 저장 | 미확정 정책·공식 ID 없음·스냅샷 허용 범위 | 미허용 저장 차단, 합의한 중복·갱신 정책만 적용 |
| 즐겨찾기 | A·B 동일 대상, A의 중복·B 항목 삭제 | 회원별 독립 저장과 소유권 보장 |
| gRPC | 정상·워밍업·지연·준비 실패 | 상태·순번·오류를 앱 규격으로 변환 |
| 다중 연결 | 서로 다른 앱 클라이언트 동시 측정 | 결과가 자기 연결에만 전달 |
| 종료 | 앱 종료·모델 종료·토큰 만료·재접속 | 이전 세션·결과 유입 방지, 자원 해제 |
| 과부하 | 빠른 입력·느린 앱 수신 | 큐·메모리 제한과 명확한 종료 |
| 배포 | 진행 중 연결이 있는 상태에서 재시작 | 종료·재접속·복구 절차 준수 |

조회·저장 검증은 실제 MySQL 동작과 차이가 나지 않도록 MySQL 격리 환경에서 확인한다. 모든 테스트가 실제 모델 서버 기동을 요구하지 않게 단위·DB 통합·gRPC 계약·실제 종단 테스트를 나눈다.

성능은 REST와 실시간을 나눠 측정한다. 평균뿐 아니라 p95·p99, 활성 연결, DB 연결 풀, 메모리, 대기열을 함께 확인한다. 목표 동시 사용자·샘플 주파수·허용 지연은 아직 정해지지 않았으며 임의 수치를 충족했다고 선언하지 않는다.

앱의 측정 시간과 서버 시간이 같은 기준이라고 가정하지 않는다. 서버 구간 지연과 앱 왕복·종단 지연은 구분해서 측정한다.

## 12. GitHub·CI/CD와 전환

저장소는 중앙 `MagNavi-server/magnavi-api-service`, 개인 포크 `ychoik/magnavi-api-service`를 사용한다.

```text
중앙 develop 최신 상태
  → 로컬 작업 브랜치
  → 개인 포크 origin에 push
  → 중앙 develop으로 PR
  → develop에서 release로 PR
  → 검증·배포
  → 안정 버전을 main에 반영
```

중앙 브랜치는 삭제·강제 push 방지와 PR 병합 규칙을 적용하고, 혼자 작업하므로 필수 승인 인원은 0명으로 운영한다. 이는 적용할 정책이며 이 문서 작성이 GitHub 설정을 변경하지는 않는다.

작업 브랜치 예시:

- `chore/project-setup`: 기본 프로젝트·환경·문서.
- `feature/database-schema`: Flyway·DB 모델.
- `feature/member-auth`: 회원·일반 인증 분리와 JWT.
- `feature/member-social-login`: 카카오·구글 인증과 선택한 계정 연결.
- `feature/place`: 실내 장소·매핑·선택적 자체 장소.
- `feature/place-search`: 네이버 API HUB 검색 연동.
- `feature/favorite`: 즐겨찾기.
- `feature/positioning-stream`: WebSocket·gRPC 연동.
- `chore/api-deployment`: 운영 설정과 CI/CD.

PR에서는 빌드·테스트·계약 생성 검증을 수행한다. 배포 자격 증명은 중앙의 신뢰된 배포 단계에서 사용하며, 개인 포크 PR 코드에 제공하지 않는다.

배포는 검증한 산출물의 버전을 식별할 수 있게 한다. DB 변경은 이전 애플리케이션과의 호환성과 복구 절차를 함께 확인하고, 애플리케이션 롤백이 DB 변경을 자동으로 되돌린다고 가정하지 않는다.

기존 API 전환 시 확인할 항목:

- 앱의 로그인 형식, API 경로, 응답 필드와 오류 처리.
- 기존 회원 비밀번호·토큰의 호환과 소셜 전용 회원 응답.
- 소셜 앱/client 설정·명시적 계정 연결 정책.
- 네이버 검색 계약과 외부 저장 허용 범위, 기존 실외 장소 API 전환.
- 장소·즐겨찾기 ID와 모델 위치 코드 매핑.
- 업무 DB의 쓰기 주체가 Spring으로 전환되는 시점.
- 실시간 연결 규격과 재접속 동작.

전환이 검증된 후 모델 서비스 담당자에게 기존 업무 API 사용 종료를 알린다. Python 쪽 파일 삭제와 정리 절차는 해당 서비스 구현 계획에서 관리한다.

## 13. 구현 전에 확정할 항목

| 항목 | 결정할 내용 |
|---|---|
| API 호환 범위 | 기존 경로·폼·필드·오류를 유지할지, 버전을 나눌지 |
| DB 시작 방식 | 기존 데이터 유지·이관 또는 개발 DB 신규 구성 |
| 회원 정보 | 연락처 필수 여부·이메일 중복 정책·일반 ID 정규화·응답 호환 |
| 소셜 로그인 | SDK·인증 흐름·허용 앱/client·계정 연결/해제 범위 |
| 외부 검색 | API HUB 신청·실제 응답·검색 화면 요구·호출 한도 |
| 외부 저장 | 캐시·즐겨찾기 스냅샷의 허용 필드·기간·갱신·삭제 |
| JWT 정책 | 식별자·키·만료·기존 토큰 호환, 재발급·로그아웃 추가 여부 |
| 장소 범위·권한 | 자체 실외 장소 관리 여부·기존 데이터 보존·검색 공개/한도·변경 권한 |
| 장소·즐겨찾기 참조 | 유형·실제 외부 ID·네이버 ID 부재·내부 FK·삭제·중복 기준 |
| 모델 코드 매핑 | 모델 버전·건물 구분과 매핑 데이터 관리 방식 |
| 앱 WebSocket 계약 | 인증 전달 방법·메시지 형식·만료·재접속 |
| gRPC 계약 | 단일 원본·버전·생성·오류·순번·측정 시간 의미 |
| 성능·한도 | 동시 사용자·주파수·큐 크기·허용 지연·연결 수 |
| 배포 | 인스턴스 수·내부 통신 인증·상태 확인·종료·롤백 |

목표 범위는 일반 로그인 유지안·카카오/구글 로그인, 실내 장소·모델 매핑, 네이버 검색, 즐겨찾기, 인증된 실시간 예측 연동이다. 외부 즐겨찾기 스냅샷은 저장 허용 조건 확인 후 적용한다. 자체 실외 장소·계정 연결/해제·회원 탈퇴·토큰 재발급·로그아웃·실내 관리자 API·분산 제한 저장소의 초기 포함 여부는 별도로 확정한다.

현재 완료 범위는 설계 문서와 빈 클래스 골격이다. 새 클래스의 메서드·필드·Spring/JPA 어노테이션·SQL은 추가하지 않았고, 기존 실행 설정·의존성도 변경하지 않았다. 실제 기능 구현·서버 기동·DB 적용·배포는 별도 단계다.
